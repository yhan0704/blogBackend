# Commit pending — Rename milestone 1 to P0 Design in requirements

## Why
- Milestone 1 only designs the **P0** features, so "Design" alone was misleading.
- A separate "P1 Design" milestone will be added later; the names need to be distinguishable.

## What changed
- `docs/requirements.md`: milestone legend row 1 renamed from `Design` to `P0 Design` and its description now says "for P0 features".
- The GitHub milestone itself is renamed to `1. P0 Design` by the user in the web UI.
